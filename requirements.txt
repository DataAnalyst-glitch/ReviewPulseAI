# --- Module 1: Data Ingestion ---
pandas>=2.0
python-dotenv>=1.0
requests>=2.31
pytest>=8.0
pypdf>=4.0  # test-only: extracts real text from generated PDFs to verify content

# --- Module 2: RAG / Vector Storage ---
langchain-core>=0.2
langchain-text-splitters>=0.2
langchain-chroma>=0.1
langchain-huggingface>=0.0.3
sentence-transformers>=2.7
chromadb>=0.5

# --- Module 3: Agentic Analysis ---
# CrewAI dropped (see CLAUDE.md deviations) — plain LangChain + Gemini instead.
langchain-google-genai>=2.0

# --- Module 4: Presentation Layer ---
streamlit>=1.35
plotly>=5.20
fpdf2>=2.7

# --- Added later, once Supabase is set up ---
# supabase>=2.4
