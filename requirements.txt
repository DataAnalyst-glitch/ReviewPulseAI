# --- Module 1: Data Ingestion ---
pandas>=2.0
python-dotenv>=1.0
requests>=2.31
pytest>=8.0

# --- Module 2: RAG / Vector Storage ---
langchain-core>=0.2
langchain-text-splitters>=0.2
langchain-chroma>=0.1
langchain-huggingface>=0.0.3
sentence-transformers>=2.7
chromadb>=0.5

# --- Module 3: Agentic Analysis ---
# CrewAI dropped (see CLAUDE.md deviations) — plain LangChain + Gemini instead.
langchain-google-genai>=2.0

# --- Added in later modules (uncomment as we build them) ---
# supabase>=2.4
# streamlit>=1.35
# fpdf2>=2.7
